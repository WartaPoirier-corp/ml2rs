; generated by drom from package skeleton 'library'

(library
  (name ml2rs_lib)
  (public_name ml2rs_lib)
  (wrapped true)
  (libraries ocaml-compiler-libs.common )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ml2rs_lib))


